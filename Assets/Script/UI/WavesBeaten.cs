using UnityEngine;
using System.Collections;

public class WavesBeaten : MonoBehaviour {
    //sets the ammount of waves beaten in a row

    public static int score = 0;

    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
